[pytest]
testpaths = tests
addopts = -q --strict-markers -p no:cacheprovider
markers =
    slow: needs a second engine or a full sweep. Excluded from the fast run;
        CI runs these nightly. Select with -m slow, exclude with -m "not slow".
filterwarnings =
    ignore::DeprecationWarning
