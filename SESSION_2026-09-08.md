# SESSION 2026-09-08 — the verification layer, and the six bugs it found

Read `STEPS_1_2_4.md` for the plan and `RESEARCH_LOG.md` for the one finding that
changed a number. This file is what happened and what it means.

---

## The headline

Kris chose items **1, 2 and 4** from `NEXT.md` and left out item 3. Item 3 turned
out to be a dependency of both the others, so it was absorbed rather than skipped.
All four are done, plus one task that was not on the list.

| | start of day | end of day |
|---|---|---|
| H-002 VWAP — gold | 6.0/10, 143.6 expected days | **6.0/10, 143.6d, every check passes** |
| H-016 ribbon — gold | 5.0/10, 175.9 expected days | **6.0/10, 127.6d, every check passes** |
| provenance | a `SUPERSEDED` note somebody typed | fingerprinted; the board flags itself |
| tests on the kernels | **zero** | **77, no skips, plus CI** |
| second-engine checked | vwap only, twice, by hand | **both, automatically** |
| commits pushed to GitHub | **none since 2026-09-06** | all of it |

**Nothing here made a strategy faster.** Both survivors are still far outside the
5–14 day target. What changed is whether their numbers can be believed.

---

## What was built

**Item 1 — kernel fingerprint and automatic staleness.** Every board record now
carries a hash of the kernels, the data, the git SHA and the cost assumption it
was produced from, and `core/build_scoreboard.py` recomputes all of it at render
time. Edit one byte of `strategies/vwap/engine.py` and H-002 renders
`3.0/10 STALE`, naming the file, with nobody editing a note. Two tiers: trade
arithmetic is a hard stale, `core/board.py` and `core/riskladder.py` are a note —
a flag that is always on gets ignored.

**The hole item 1 left, closed the same day.** The fingerprint hashes what a
manifest *declares*, so an undeclared kernel never trips the flag.
`core/manifest_audit.py` walks the import graph statically and fails on anything
reachable but undeclared.

**Item 2 — the verification gate.** `core/verification.py` runs the suite per
hypothesis and writes `verification.json`, stamped with the kernel hash the
evidence was earned on; `core/scorecard.py` caps an unverified record at 3.0.
A **skip is not a pass** — the gate reads junit XML, because "all passed" and
"all skipped" both exit 0.

**Item 3, absorbed.** `pytest` + `hypothesis`, `tests/` with a shared kernel
adapter, golden trade lists, and the repo's first CI.

**Item 4 — one `Strategy` interface.** `core/strategy.py`; both kernels declare
themselves; the tests run *through* the declarations. Step 5 (extracting shared
code) was **measured and declined**: it is one line each per kernel, and
`tests/test_costs.py` already protects the semantics behaviourally.

**Task A, not on the list — the ribbon kernel through a second engine.**
It became the top item the moment item 2 shipped, because it was the only thing
capping H-016. **108 of 108 rule shapes match exactly** across four timeframes.

---

## The six bugs

Five were found by the tests, not by reading code.

| # | bug | how found |
|---|---|---|
| 1 | `stage18_goldbook.py` rewrote `board.json` **on import** — `stage20 --board` silently wrote the superseded pre-fix record first | wiring item 1 |
| 2 | The live ribbon board is written by `stage11_reprice.py`, not `stage10_board.py`; running stage 10 restores the four-leg book **containing silver** | running it |
| 3 | Both kernels **crash on an empty series** — and `load_tf` returns one | `test_degenerate` |
| 4 | `strategies/ribbon/test_parity.py` had **never been run by anything** — named like a test, is a script | building the gate |
| 5 | **The dead-bar fix never guarded the EXIT** | reading ribbon to port it |
| 6 | `self.stop` overwrote NautilusTrader's `Strategy.stop()` | the port's first run |

### Bug 5 is the one that moved a number

`RESEARCH_LOG.md` has it in full. Short version: the 2026-09-07 work guarded the
decision bar and the fill bar and stopped. **1,079 of H-002's 17,432 exits and
1,532 of H-016's 8,610 landed on a bar where nothing traded.**

**Which field of the bar the exit rule reads decides how much it cost.** A
trailing stop tests every bar's `high` and `low`, and a padded weekend bar has
`hi == lo ==` the frozen price — so it sits on the trail and force-closes a live
trade. A session-horizon time exit reads only the `close`, and a padded bar's
close *is* the last real close. Same bug; H-016 moved a long way and H-002 did
not move at all.

**And the first estimate had the sign backwards.** Subtracting the R of the
affected trades said dead-bar exits carried +59.03R of H-016's +127.08R — i.e.
that fixing it would halve the edge. Re-running did the opposite: they were
premature stop-outs at a price nobody traded. **The counterfactual to a
fill-assumption bug is a re-run, not a subtraction.**

---

## What did NOT get done, and what it would take

* **The gap-through fill.** A position that rides through the weekend resolves on
  the first bar that trades. If that bar *opens* beyond the stop, both kernels
  still fill **at the stop price**. That is optimistic, it is unmeasured, and it
  is now the largest known unpriced assumption in the book.
* **Anything about pace.** See below.
* **Blocked on Kris:** which prop firms (worth 17pp of pass rate), and the exact
  Power of Three entry/stop/target rules.

---

## The thing this session did not touch

Both survivors need **143.6** and **127.6** expected days against a **5–14 day**
target. Every lever already tried on them: combining cells, repricing at measured
cost, raising risk (no headroom — the max-loss cap binds first). Widening the
book was tested and made it slower (H-012).

Speed has to come from a **genuinely faster mechanism**. Per the standing pattern
in `CLAUDE.md`, that means a **data feed**, not another price geometry — twelve
price hypotheses have died here and the one crypto book that looked alive died to
a look-ahead fix.

The freeze on new hypotheses is now satisfied. That is the next conversation.
